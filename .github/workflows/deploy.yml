name: Deploy FM Operations

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  NODE_VERSION: '20'

jobs:
  # ============================================================
  # Validate — jalankan di setiap push & PR
  # ============================================================
  validate:
    name: ✅ Validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install API deps
        run: npm install
        working-directory: api

      - name: Install Frontend deps
        run: npm install
        working-directory: frontend

      - name: Check required files
        run: |
          test -f api/src/index.js         && echo "✅ api/src/index.js"
          test -f api/wrangler.toml        && echo "✅ api/wrangler.toml"
          test -f frontend/src/app.js      && echo "✅ frontend/src/app.js"
          test -f frontend/public/index.html && echo "✅ frontend/public/index.html"
          test -f schema/migrations.sql    && echo "✅ schema/migrations.sql"

  # ============================================================
  # Deploy API ke Cloudflare Workers
  # ============================================================
  deploy-api:
    name: 🚀 Deploy API (Workers)
    runs-on: ubuntu-latest
    needs: validate
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install API deps
        run: npm install
        working-directory: api

      - name: Deploy Worker ke Cloudflare
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: api
          command: deploy
          secrets: |
            JWT_SECRET
        env:
          JWT_SECRET: ${{ secrets.JWT_SECRET }}

  # ============================================================
  # Migrasi Database D1
  # ============================================================
  migrate-db:
    name: 🗄️ Migrate D1 Database
    runs-on: ubuntu-latest
    needs: deploy-api
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install Wrangler
        run: npm install -g wrangler@3

      - name: Run DB Migrations (remote D1)
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          wrangler d1 execute fm-operations-db \
            --remote \
            --file=schema/migrations.sql \
            --yes
        # continue-on-error: true karena CREATE IF NOT EXISTS aman dijalankan ulang
        continue-on-error: true

  # ============================================================
  # Build & Deploy Frontend ke Cloudflare Pages
  # ============================================================
  deploy-frontend:
    name: 🌐 Deploy Frontend (Pages)
    runs-on: ubuntu-latest
    needs: validate
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install Frontend deps
        run: npm install
        working-directory: frontend

      - name: Install esbuild
        run: npm install -g esbuild

      - name: Build frontend
        working-directory: frontend
        env:
          API_BASE_URL: ${{ secrets.API_BASE_URL }}
        run: node build.js

      - name: Deploy ke Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: frontend
          command: pages deploy dist --project-name=fm-operations --commit-dirty=true

  # ============================================================
  # Preview Deploy untuk Pull Request
  # ============================================================
  preview-frontend:
    name: 👁️ Preview (PR)
    runs-on: ubuntu-latest
    needs: validate
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install Frontend deps
        run: npm install
        working-directory: frontend

      - name: Install esbuild
        run: npm install -g esbuild

      - name: Build frontend (preview)
        working-directory: frontend
        env:
          API_BASE_URL: ${{ secrets.API_BASE_URL }}
        run: node build.js

      - name: Deploy Preview ke Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: frontend
          command: pages deploy dist --project-name=fm-operations --branch=pr-${{ github.event.pull_request.number }}
