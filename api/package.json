{
  "name": "fm-operations-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required for API'",
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "wrangler": "^3.78.0"
  },
  "dependencies": {
    "papaparse": "^5.5.4"
  }
}
